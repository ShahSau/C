#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* creating a variable 
    int age = 0;
    /* printg the promt
    printf("Enter your age: ");
    /*take the age from user and store it
    scanf("%d", &age);
    /*print the age
    printf("Your age is: %d ", age); 
    getting a floating numer
    double gpa= 0.0;
    printf("Enter your age: ");
    scanf("%lf", &gpa);
    printf("Your age is: %lf ", gpa); */

    /*getting a string*/
    char name[20];
    printf("Enter your name: ");
    scanf("%s", name);
    printf("Your name is: %s %s", name); 
    return 0;
}